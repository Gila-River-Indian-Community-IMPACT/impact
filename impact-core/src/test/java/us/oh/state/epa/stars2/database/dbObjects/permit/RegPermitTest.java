package us.oh.state.epa.stars2.database.dbObjects.permit;
/*
import static org.junit.Assert.assertTrue;
import static org.junit.Assert.fail;

import org.junit.Ignore;
import org.junit.Test;
// Junit test suite for the {@link RegPermit} class.
// 
// @author Charles Meier (cmeier@acm.org)
// @version 1

public class RegPermitTest {

	// TODO: Fix against database
    
    //Make sure that validation messages are correctly handled as fields are
    // modified.
   
	@Test
	@Ignore
    public void testRegPermitValid() {

        RegPermit permit = new RegPermit();
        assertTrue(
                "RegPermitTest.testRegPermitValid() Valid permit is invalid.",
                permit.isValid());

    }

   
     // Test the {@link RegPermit#equals(Object obj)} and
     //{@link RegPermit#hashCode()} methods.
    
	@Test
    public void testRegPermitEquals() {

        try {
        } catch (Exception e) {
            e.printStackTrace();
            fail("NoteTest.testRegPermitEquals() threw an unexpected exception"
                    + e.getMessage());
        }

    }


}
*/
